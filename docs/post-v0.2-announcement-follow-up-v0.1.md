# Post-v0.2 Announcement Follow-up v0.1

Status: Phase 24 draft follow-up copy.

These are draft-only follow-up notes. They are not automatically posted.

## Core Message

SpecWise v0.2.0-preview.0 is now available.

It adds a second realistic mock example, deterministic draft polish, CSV field inference, CLI/DX improvements, and first-time docs simplification.

SpecWise v0.2.0-preview.0 已发布，新增第二个真实感 mock 示例，并改进 deterministic draft、CSV 字段推断、CLI/DX 和新手文档。

## LinkedIn Follow-up

SpecWise v0.2.0-preview.0 is now available as a GitHub pre-release.

This preview adds a second realistic mock example for legacy CRM follow-up, improves deterministic draft quality, adds CSV field inference polish, sharpens CLI/DX, and simplifies the first-time docs path.

The boundary is unchanged: local-first, review-required, no real AI provider calls, no coding agent execution, no generated application code, and no npm publish.

Release:
https://github.com/darwinx687-afk/specwise/releases/tag/v0.2.0-preview.0

## X/Twitter Follow-up

SpecWise v0.2.0-preview.0 is out as a GitHub pre-release.

New in this preview:
- second realistic mock example
- deterministic draft polish
- CSV field inference improvements
- CLI/DX polish
- simpler first-time docs

Still local-first and review-required. No npm publish.

https://github.com/darwinx687-afk/specwise/releases/tag/v0.2.0-preview.0

## 小红书更新

SpecWise v0.2.0-preview.0 已发布。

这次主要补强了第一次试用体验：

- 新增第二个真实感 mock 示例：Legacy CRM Follow-up
- 改进 deterministic draft 的可读性和覆盖度
- 改进 CSV 字段推断
- 改进 CLI 错误提示和 first-run DX
- 新增 Start Here 和命令选择文档

边界不变：本地优先、需要人工审查、不调用真实 AI provider、不调用 coding agent、不生成应用代码、不发布 npm。

## 掘金更新段落

SpecWise v0.2.0-preview.0 已作为 GitHub pre-release 发布。本轮不是大而全的新功能，而是把 v0.1 preview 里最影响第一次体验的部分继续收紧：新增 Legacy CRM Follow-up 作为第二个真实感 mock 示例，改进 deterministic draft、CSV 字段推断、CLI/DX，以及 first-time docs。当前边界仍然是 local-first、review-required，不调用真实 AI provider，不调用 coding agent，不生成应用代码，也不发布 npm。

Release:
https://github.com/darwinx687-afk/specwise/releases/tag/v0.2.0-preview.0

## GitHub Release Follow-up Note

Thanks for checking out SpecWise v0.2.0-preview.0.

Feedback that would be most useful:

- which first-run command or doc was confusing
- whether the CRM example makes the workflow clearer
- whether generated draft wording feels too confident or too vague
- which local, deterministic improvement would help next
- whether any safety boundary is unclear

Please avoid sharing sensitive business data, private screenshots, API keys, customer data, employee data, or internal company documents in public issues.
