# v0.2 Release Decision Record

Status: Phase 23 release decision record.

这是发布决策记录，记录本阶段授权创建 v0.2 preview tag 和 GitHub pre-release。

## Decision

Create `v0.2.0-preview.0` as the next preview release after Phase 22A-22E.

## Reason

v0.2 now has a second realistic example, better deterministic draft quality, better CSV field inference, better CLI/DX, and clearer first-time docs.

## Release Type

GitHub pre-release.

## npm

Do not publish. `package.json` remains private.

## Tag

Create in Phase 23 after final validation. Target tag is `v0.2.0-preview.0`.

## Status

Approved for GitHub pre-release creation.

## Boundaries

- Phase 23 creates only the `v0.2.0-preview.0` tag.
- Phase 23 creates only the matching GitHub pre-release.
- Phase 23 does not publish npm.
- Phase 23 does not add runtime features.
- Phase 23 does not call real AI providers or coding agents.
- Phase 23 does not generate application code or final spec-packs.
