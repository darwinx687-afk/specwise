# v0.2 Release Decision Record

Status: Phase 22F release decision record.

这是发布决策记录，不代表已经创建 tag 或 release。

## Decision

Prepare `v0.2.0-preview.0` as the next preview release candidate after Phase 22A-22E.

## Reason

v0.2 now has a second realistic example, better deterministic draft quality, better CSV field inference, better CLI/DX, and clearer first-time docs.

## Release Type

GitHub pre-release candidate.

## npm

Do not publish. `package.json` remains private.

## Tag

Do not create in Phase 22F. Candidate tag is `v0.2.0-preview.0`.

## Status

Prepared, pending owner approval.

## Boundaries

- Phase 22F does not create the tag.
- Phase 22F does not create the GitHub release.
- Phase 22F does not publish npm.
- Phase 22F does not add runtime features.
- Phase 22F does not call real AI providers or coding agents.
- Phase 22F does not generate application code or final spec-packs.
