# v0.1 Preview Tag Plan

Status: Phase 15 planning document. This does not create a tag, release, or npm publication.

## Target

- Target tag: `v0.1.0-preview.0`
- Target release title: `SpecWise v0.1.0-preview.0`

## Preconditions

- [ ] License chosen.
- [ ] `LICENSE` file added.
- [ ] `package.json` license field updated if appropriate.
- [ ] `npm test` passes.
- [ ] `npm run smoke` passes.
- [ ] Hygiene scan passes.
- [ ] README safety boundaries still visible.
- [ ] `CHANGELOG.md` updated.
- [ ] Release notes draft reviewed.

## Do Not Tag Until

- [ ] License is confirmed.
- [ ] Project owner approves tag creation.
- [ ] Project owner approves GitHub release creation.

## Phase 15 Boundary

Phase 15 does not create the tag.

Phase 15 does not create the GitHub release.

Phase 15 does not publish npm.

Phase 15 does not add a `LICENSE` file or modify `package.json` license metadata.

## Future Execution Sketch

Only after the owner confirms the license and release approval:

```bash
npm test
npm run smoke
git status --short
git tag v0.1.0-preview.0
git push origin v0.1.0-preview.0
```

The commands above are planning notes only. They are not run in Phase 15.
